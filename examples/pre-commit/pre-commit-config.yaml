repos:
  - repo: local
    hooks:
      - id: lfguard-check-desired
        name: lfguard check desired policy
        entry: lfguard check --desired policy/desired.yaml --fail-on-findings
        language: system
        pass_filenames: false
        files: ^policy/desired\.(json|ya?ml)$
